---
phase: 02-clicktrade
plan: 01
type: execute
wave: 1
depends_on: [01-05]
files_modified:
  - service/backend/src/controllers/options.controller.js
  - service/frontend/src/components/ClickTrade.jsx
autonomous: true
requirements: [P2-R01, P2-R02-partial]

must_haves:
  truths:
    - "GET /api/options/chain calls Kotak REST API when session.isAuthenticated(), falls back to stub on error or unauthenticated"
    - "GET /api/options/expiries returns 4 upcoming weekly Thursdays computed correctly for any day-of-week"
    - "ClickTrade.jsx fetches expiries on underlying change and shows an expiry dropdown"
    - "Selected expiry is passed to OptionChain component which forwards it to the API call"
    - "npm run build in service/frontend completes without errors"
  artifacts:
    - path: "service/backend/src/controllers/options.controller.js"
      provides: "Kotak option chain proxy with stub fallback"
    - path: "service/frontend/src/components/ClickTrade.jsx"
      provides: "Expiry selector wired to OptionChain"
  key_links:
    - from: "service/frontend/src/components/ClickTrade.jsx"
      to: "service/frontend/src/components/OptionChain.jsx"
      via: "expiry prop"
      pattern: "<OptionChain symbol={underlying} expiry={expiry} />"
    - from: "service/backend/src/controllers/options.controller.js"
      to: "service/backend/src/brokers/kotak/session.js"
      via: "session.isAuthenticated()"
      pattern: "auth guard before Kotak fetch"
---

<objective>
Implement P2-R01: real Kotak option chain HTTP fetch in options.controller.js.
Also close the expiry selector gap in ClickTrade.jsx (P2-R02 partial).

Current state: options.controller.js has stub data + TODO comment for real Kotak call.
ClickTrade.jsx has no expiry picker — getDTE() hardcodes nearest Thursday.

What's pre-existing and does NOT need building:
- Leg builder UI (P2-R02 ✅ in ClickTrade.jsx)
- Black-Scholes Greeks per leg (P2-R03 ✅ calcLegGreeks + greeks npm)
- Net portfolio Greeks display (P2-R04 ✅ GreeksTable component)
- Payoff graph (P2-R05 ✅ PayoffGraph.jsx + recharts)
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement options.controller.js</name>
  <files>
    service/backend/src/controllers/options.controller.js
  </files>
  <action>
Replace stub-only controller with:
- getChain: try Kotak GET /rest/neo/v1/option-chain with Bearer auth when authenticated.
  Parse response (Kotak format: { data: { oc: [{strikeprice, CE:{ltp,oi,iv}, PE:{ltp,oi,iv}}], spotprice } }).
  On parse fail or error, fall through to _stubChain().
- getExpiries: compute 4 upcoming weekly Thursdays using (4 - day + 7) % 7 formula.
  Fix: current stub has loop bug causing duplicate expiry dates.
- Extract _kotakHeaders(), _parseKotakChain(), _stubChain() as module-private functions.
- Use axios (already in backend package.json).
  </action>
  <done>
    - getChain calls Kotak when isAuthenticated() = true
    - getChain returns stub when not authenticated or on any error
    - getExpiries returns 4 correct weekly Thursdays
    - module.exports = optionsController (unchanged shape)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add expiry selector to ClickTrade.jsx</name>
  <files>
    service/frontend/src/components/ClickTrade.jsx
  </files>
  <action>
Three targeted changes:
1. Add state: expiry (string, ''), expiries (array)
2. Add useEffect: fetch /api/options/expiries?symbol={underlying} on underlying change,
   populate expiries, set expiry to expiries[0]
3. Add expiry <select> in header row (next to underlying buttons)
4. Pass expiry={expiry} to <OptionChain symbol={underlying} expiry={expiry} ...>
  </action>
  <done>
    - Expiry dropdown shows 4 Thursday dates
    - Changes underlying resets expiry to first available
    - OptionChain receives expiry prop
  </done>
</task>

</tasks>

<verification>
```bash
# Backend: module loads without error
node -e "require('./src/controllers/options.controller'); console.log('OK')"

# Frontend: build
cd service/frontend && npm run build
```
</verification>

<success_criteria>
- GET /api/options/chain?symbol=NIFTY returns { symbol, expiry, spot, strikes } with stub data when unauthenticated
- GET /api/options/expiries?symbol=NIFTY returns 4 future Thursdays, none duplicate
- ClickTrade expiry dropdown visible and functional
- npm run build clean
</success_criteria>

<output>
After completion, create .planning/phases/02-clicktrade/02-01-SUMMARY.md
Update STATE.md: Phase 2 current plan = 01, status = Complete. Mark pre-existing plans 02-03/04/05 as done.
</output>
